<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 无存储权限：本应用不读写外部存储（录音功能未启用） -->

    <!-- ===== 电视兼容声明 =====
         这两条决定「能不能装到电视上」，漏了就是装不上或装了找不到图标，
         而且报错信息很不直观（INSTALL_FAILED_NO_MATCHING_ABIS 之类完全指错方向）。

         touchscreen required=false：很多电视盒子/智能电视没有触摸屏，
         默认 required=true 会让 Play/安装器直接判定「设备不兼容」而拒绝安装。
         本应用全部交互都能用遥控器完成，不依赖触摸。

         leanback required=false：电视上「没有遥控器也能用」的声明。
         写了它，Play 才会把应用算作电视可用。 -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- 电视桌面的入口。没有这一条，装上了也不在电视主界面显示，
                     只能靠 adb 命令启动 —— 对「装在盒子里用遥控器开」来说等于没用。 -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:label="@string/settings"
            android:parentActivityName=".MainActivity" />

        <service
            android:name=".RadioService"
            android:exported="false" />

        <receiver
            android:name=".TimerReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
